<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CyberGuard - Logs Analyzer</title>
    <link rel="stylesheet" href="style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <header class="header">
            <div class="logo">
                <i class="fas fa-shield-alt"></i>
                <h1>CyberGuard</h1>
            </div>
            <div class="status">
                <span class="status-indicator" id="statusIndicator"></span>
                <span id="statusText">Connecting...</span>
            </div>
        </header>

        <div class="controls">
            <div class="search-box">
                <i class="fas fa-search"></i>
                <input type="text" id="searchInput" placeholder="Search IP, action, user...">
            </div>
            <button id="refreshBtn" class="btn">
                <i class="fas fa-sync-alt"></i> Refresh
            </button>
            <button id="generateBtn" class="btn secondary">
                <i class="fas fa-plus"></i> Generate Logs
            </button>
        </div>

        <div class="dashboard" id="dashboard">
            <!-- Stats cards populated by JS -->
        </div>

        <div class="main-content">
            <div class="panel threats-panel">
                <h3><i class="fas fa-exclamation-triangle"></i> Threats</h3>
                <div id="threatsList"></div>
            </div>

            <div class="panel logs-panel">
                <h3><i class="fas fa-list"></i> Live Logs <span id="logsCount"></span></h3>
                <table id="logsTable">
                    <thead>
                        <tr>
                            <th>Time</th>
                            <th>Src IP</th>
                            <th>Dst IP</th>
                            <th>Action</th>
                            <th>Port</th>
                            <th>Bytes</th>
                            <th>Threat</th>
                        </tr>
                    </thead>
                    <tbody id="logsBody"></tbody>
                </table>
            </div>
        </div>
    </div>

    <script src="script.js"></script>
</body>
</html>